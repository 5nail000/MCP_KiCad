{
  "mcpServers": {
    "kicad": {
      "type": "stdio",
      "command": "${workspaceFolder}/.venv/Scripts/python.exe",
      "args": ["-m", "kicad_ai.mcp.server"],
      "env": {
        "KICAD_AI_WORKSPACE": "${workspaceFolder}"
      }
    }
  }
}
